.b-list__act.item-link
  .b-list__h.b-list__h_day
    .b-list__body.b-list__body_arrow-right
      .b-list__day-h.b-list__day-h_size_sm <%= name %>
        .b-list__day-place <%= placeName %>
        .b-list__day-time
          | <%= _.template.formatDate('hh:mm', new Date(start * 1000)) %>
          | <% if (end) { %>
          =' '
          |   – <%= _.template.formatDate('hh:mm', new Date(end * 1000)) %>
          | <% } %>
      .b-list__arrow-right
        svg
          use(xlink:href='#icon_star')

